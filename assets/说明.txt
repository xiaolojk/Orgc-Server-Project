# 收款码图片放这里

请把你的收款二维码图片放到这个 `assets/` 目录，文件名必须为：

| 文件名 | 用途 |
| --- | --- |
| `pay-wechat.png` | 微信收款码 |

- 当前网站只使用微信收款，无需支付宝。
- 图片建议用清晰的方图（例如 300×300 以上的 PNG/JPG）。
- 放好并提交推送到 `main` 后，网站会自动重新部署，付款弹窗即可显示你的收款码。

> 接收申请 / 开通消息的邮箱，请在仓库根目录 `script.js` 顶部的 `SHOP.email` 中修改。